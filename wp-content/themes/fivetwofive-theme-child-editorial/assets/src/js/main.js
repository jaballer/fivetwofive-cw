/**
 * FiveTwoFive Editorial Child — front-end scripts.
 *
 * Intentionally minimal. Behavior is added per-feature in later PRs; the
 * parent theme already handles primary navigation.
 */
( function () {
	'use strict';
}() );
